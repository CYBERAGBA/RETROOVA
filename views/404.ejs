<%- include('./layouts/main', {
    title: t('messages.notFound', 'Page non trouvée'),
    metaDescription: t('messages.notFound', 'Page non trouvée'),
    canonicalUrl: typeof canonicalUrl !== 'undefined' ? canonicalUrl : siteUrl,
    body: `
<div class="error-container">
    <div class="error-card">
        <h1 class="error-code">404</h1>
        <h2>${t('messages.notFound', 'Page non trouvée')}</h2>
        <p>${t('messages.notFound', 'Page non trouvée')}</p>
        <a href="/" class="btn btn-primary">${t('messages.returnHome', "Retourner à l'accueil")}</a>
    </div>
</div>
`
}) %>
